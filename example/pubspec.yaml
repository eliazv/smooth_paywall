name: smooth_paywall_example
description: Example app for smooth_paywall.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  smooth_paywall:
    path: ../
  smooth_bottom_sheet:
    path: ../../smooth_bottom_sheet

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
