name: smooth_paywall
description: "A modern, customizable paywall UI for Flutter apps with optional smooth_bottom_sheet integration."
version: 0.0.2
homepage: https://github.com/eliazv/smooth_paywall
repository: https://github.com/eliazv/smooth_paywall
issue_tracker: https://github.com/eliazv/smooth_paywall/issues
topics:
  - paywall
  - subscriptions
  - monetization
  - flutter-widgets
  - smooth

environment:
  sdk: ^3.10.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
